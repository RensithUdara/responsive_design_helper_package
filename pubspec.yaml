name: responsive_design_helper
description: A Flutter package providing utilities for responsive design across different screen sizes.
version: 0.0.2
homepage: https://github.com/RensithUdara/responsive_design_helper_package

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
