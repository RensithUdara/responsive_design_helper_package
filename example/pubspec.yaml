name: responsive_design_helper_example
description: An example app demonstrating the use of responsive_design_helper.
version: 0.0.1

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  responsive_design_helper:

dev_dependencies:
  flutter_test:
    sdk: flutter
